package com.novaplay.tv.data.remote

/** Compile-time release counterpart. Release code never receives mock assignments. */
object MockPortal {
    val policy: PortalPolicyDto? = null
    val playlists: List<PortalPlaylistDto> = emptyList()
}
